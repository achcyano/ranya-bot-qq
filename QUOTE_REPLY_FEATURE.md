# Quote Reply 功能说明

## 概述

本功能参考 [XiaoMengXinX/Telegram_QuoteReply_Bot-Go](https://github.com/XiaoMengXinX/Telegram_QuoteReply_Bot-Go) 实现了引用回复功能，允许用户通过简单的命令生成有趣的互动消息。

## 功能特性

### 基本格式

1. **标准格式**：`/动词 [补充内容]`
   - 示例：`/踢` → "@发送者 踢了 自己！"
   - 示例：`/拥抱 轻轻地` → "@发送者 拥抱 自己 轻轻地！"

2. **回复消息**：回复某条消息并发送 `/动词 [补充内容]`
   - 示例：回复用户A的消息，发送 `/踢` → "@发送者 踢了 @用户A！"
   - 示例：回复用户A的消息，发送 `/抱抱 紧紧地` → "@发送者 抱抱 @用户A 紧紧地！"

3. **反转主客体**：使用反斜杠 `\` 替代 `/`
   - 示例：回复用户A的消息，发送 `\踢` → "@用户A 踢了 @发送者！"
   - 这在想表达"被对方XX"的场景很有用

4. **指定目标用户**：`/动词@QQ号 [补充内容]`
   - 示例：`/踢@123456789` → "@发送者 踢了 @123456789！"
   - 示例：`/表扬@123456789 做得很好` → "@发送者 表扬 @123456789 做得很好！"

### 特殊说明

- 为了兼容中文动词，命令的第二个字符如果是 ASCII 字符（如常规的 `/help`），将不会触发此功能
- 如果需要使用 ASCII 字符作为动词，可以在 `/` 或 `\` 后面添加 `$` 符号，如 `/$kick`
- 系统会自动@相关用户，让消息更有互动感

## 使用示例

### 示例1：基础使用
```
用户: /拥抱
机器人: @用户 拥抱了 自己！
```

### 示例2：回复消息
```
用户A: 今天天气真好
用户B: (回复用户A) /赞同
机器人: @用户B 赞同了 @用户A！
```

### 示例3：添加补充内容
```
用户A: 我做了一道菜
用户B: (回复用户A) /表扬 做得真棒
机器人: @用户B 表扬 @用户A 做得真棒！
```

### 示例4：反转主客体
```
用户A: 你好
用户B: (回复用户A) \打招呼
机器人: @用户A 打招呼了 @用户B！
```

### 示例5：指定QQ号
```
用户: /感谢@123456789 帮了大忙
机器人: @用户 感谢 @123456789 帮了大忙！
```

## 技术实现

- **插件类**：`QuoteReplyPlugin.kt`
- **框架**：基于 Shiro QQ 机器人框架
- **消息格式**：使用 CQ 码格式进行用户@提及
- **匹配逻辑**：
  - 检测消息开头的 `/` 或 `\`
  - 解析命令和参数
  - 根据回复消息或@提及确定目标用户
  - 构建格式化的回复消息

## 注意事项

1. 此功能仅在群聊中生效
2. 需要机器人有发送消息的权限
3. @功能依赖于正确的CQ码格式
4. 用户名优先使用群名片，其次使用昵称

## 与原版差异

相比 Telegram 版本的实现，QQ 版本做了以下适配：

1. 使用 CQ 码替代 Markdown 格式进行用户提及
2. 使用 QQ 号进行用户识别
3. 针对 QQ 群消息的特性进行了优化
4. 简化了用户名获取逻辑（不需要抓取网页）

## 贡献

本功能参考了 [XiaoMengXinX/Telegram_QuoteReply_Bot-Go](https://github.com/XiaoMengXinX/Telegram_QuoteReply_Bot-Go) 的设计思路并适配到 QQ 平台。感谢原作者的创意！
